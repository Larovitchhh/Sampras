[project]
name = "sampras"
description = "Create a project from scratch: Sampras token"
authors = []
telemetry = true
cache_dir = "./.cache"

# [contracts.sampras]
# path = "contracts/sampras.clar"

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
